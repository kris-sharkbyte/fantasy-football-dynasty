{
  "name": "functions",
  "private": true,
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "_comment": "Note: Node 22 is experimental for Firebase Functions. For production stability, consider using Node 20 instead.",
  "scripts": {
    "lint": "echo 'Linting disabled for now'",
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "openai": "^6.15.0"
  }
}
